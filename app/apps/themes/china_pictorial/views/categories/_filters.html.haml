%h1.text-white FILTERS
.h3.text-center Refine your search

= form_tag(cpsearch_path, method: :post, class: "form-search form-horizontal search-form") do

  %label{for: 'search-input'}
    %i.fa.fa-search{aria: {hidden: true}}
    %span.sr-only= t("camaleon_cms.common.search")
  
  = text_field_tag :q, params[:q], id: 'search-input', class: 'form-control input-lg', placeholder:"#{t("camaleon_cms.common.search")}...", autocomplete: "off", spellcheck: "false", autocorrect: "off", tabindex:"1"

  = link_to '/', class: "fa fa-times-circle #{params[:q].nil? ? 'invisible' : ''}", aria: {hidden: true}, id: 'search-clear' do
    %span.sr-only Clear search
    
  %h2 Post published since
  
  - { "one day" => 1, "one week" => 7, "one month" => 30}.each do |label, days|
    .form-check.form-check-inline
      %label.form-check-label{for: "published_#{days.to_s}"}
        = radio_button_tag :published_after, days.to_s, value: days, class: 'form-check-input'
        = label.capitalize
        
  %h2 Categories
  
  - CamaleonCms::PostType.find_by_slug('post').categories.each do |category|
    .form-check.form-check-inline
      %label.form-check-label{for: "category_#{category.slug}"}
        = check_box_tag 'categories[]', category.id, params[:categories] && params[:categories].include?(category.id.to_s), class: 'form-check-input', id: "category_#{category.slug}"
        = category.decorate.name


  %button.btn.btn-primary