%h3.mb-2= link_to post.the_title, post.the_path

%p.font-italic
  %span{class: "text-#{color}"}
    = t :by, scope: :cid
  %span.text-dark-grey
    = post.get_field(:author) || post.the_author.fullname
    = l post.published_at.to_date, format: :long

%p= post.the_excerpt

- if thumbnail

  = post.the_thumb(class: 'img-fluid mb-5')
